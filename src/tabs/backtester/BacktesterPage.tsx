export default function BacktesterPage({ drawerOpen }: { drawerOpen: boolean }) {
  void drawerOpen // stub — real page lands with the backtester UI
  return <div className="p-6 text-gray-500 text-sm">Backtester coming up.</div>
}
